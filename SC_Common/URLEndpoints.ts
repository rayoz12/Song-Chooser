export enum RootEndpoints {
    TEMPLATE = "/template"
}

export enum TemplateEndpoints {
    GET_ALL = "/", //GET on /
    GET_BY_ID = "/", //GET on /<ID>
    CREATE = "/", //POST on /
    SAVE = "/", //PUT on /
}